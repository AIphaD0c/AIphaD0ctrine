# Identity

Identity is modeled as a reversible runtime state, not a binary switch.

Suggested states:

- observer
- aligned participant
- embodied performer
- distributed performer
- fractured identity
- reoriented participant

Transitions require explicit safeguards and a deterministic exit path.
